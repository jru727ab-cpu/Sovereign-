<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal theme — no WindowInsets customisation needed for MVP. -->
    <style name="Theme.Civiltas" parent="android:Theme.Material.NoTitleBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
